# write a program that returns a list
# that contains only the elements that
# are common between the lists

lis = []
siz = input("Enter the size of the list :")
for i in range(0, siz):
    nun = input("Enter %d element: " % (i + 1))
    lis.append(nun)
print lis
